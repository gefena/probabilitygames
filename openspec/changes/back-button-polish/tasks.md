## 1. GamePageLayout — restyle button as pill chip

- [x] 1.1 In `src/components/GamePageLayout.jsx`, replace the button classes with: `mb-4 py-3 px-4 bg-white rounded-full shadow-sm border border-slate-200 text-violet-600 font-semibold hover:shadow-md transition-shadow text-sm`

## 2. i18n — update label

- [x] 2.1 In `src/i18n/en.json`, change `common.backHome` from `"← Home"` to `"← Games"`
- [x] 2.2 In `src/i18n/he.json`, change `common.backHome` from `"← בית"` to `"→ משחקים"` (RTL: "back" points right)
