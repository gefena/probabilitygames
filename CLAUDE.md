# Project Conventions

## Deployment

This project is live on Vercel, deployed automatically from the `main` branch on GitHub (`gefena/probabilitygames`).

After completing an `/opsx:apply` session, commit and push all changes to `main` so the Vercel deployment stays in sync.
